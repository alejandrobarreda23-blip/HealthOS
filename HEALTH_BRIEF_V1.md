# Health Brief V1

A compact deterministic interface between HealthOS and any reasoning model. It contains data quality, recovery, sleep, training, body, events, active findings, uncertainty and provenance. It explicitly retains insufficient-data states and never fills missing wearable periods with historical values.

AI may explain, relate, form hypotheses and request more data; the brief, baseline and findings are generated deterministically before LLM reasoning.
