export type AssignmentRule='measurement_local_date'|'start_date'|'end_date'|'wake_date'|'provider_date';function localDate(d:Date,tz:string){const p=new Intl.DateTimeFormat('en-CA',{timeZone:tz,year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(d);const m=Object.fromEntries(p.map(x=>[x.type,x.value]));return `${m.year}-${m.month}-${m.day}`}export function assignPhysiologicalDate(i:{startedAt:Date;endedAt?:Date;timezone:string;providerDate?:string},r:AssignmentRule){if(r==='wake_date'||r==='end_date'){if(!i.endedAt)throw Error('endedAt required');return localDate(i.endedAt,i.timezone)}if(r==='provider_date'){if(!i.providerDate)throw Error('providerDate required');return i.providerDate}return localDate(i.startedAt,i.timezone)}