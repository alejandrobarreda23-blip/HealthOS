import{convertLabValue,type LabMetric}from'./units';import type{PhenoAgeInputs}from'../aging/phenoage';
export interface RawLab{metric:LabMetric;value:number;unit:string}
export function preparePhenoAgeInputs(ageYears:number,labs:RawLab[]):{inputs:PhenoAgeInputs;conversions:ReturnType<typeof convertLabValue>[]} {
 const conversions=labs.map(x=>convertLabValue(x.metric,x.value,x.unit));const bad=conversions.filter(x=>!x.plausible);if(bad.length)throw new Error(`Implausible converted lab values: ${bad.map(x=>x.metric).join(', ')}`);const m=new Map(conversions.map(x=>[x.metric,x.value]));const req=(k:LabMetric)=>{const v=m.get(k);if(v==null)throw new Error(`Missing PhenoAge lab: ${k}`);return v};return{inputs:{ageYears,albuminGL:req('albumin'),creatinineUmolL:req('creatinine'),glucoseMmolL:req('glucose'),crpMgDl:req('crp'),lymphocytePercent:req('lymphocyte_percent'),mcvFl:req('mcv'),rdwPercent:req('rdw'),alkalinePhosphataseUL:req('alkaline_phosphatase'),wbc10e3Ul:req('wbc')},conversions}}
