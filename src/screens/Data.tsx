import{useAuth}from'../auth/AuthProvider';import{isLiveMode}from'../state/runtime';
export default function Data(){const{user,signOut}=useAuth();const sources=[['Health Connect','Preparado','Actividad, sueño y signos vitales'],['Oura','Próximamente','API directa'],['Ultrahuman','Próximamente','UltraSignal API'],['Suunto Race','Vía Health Connect','Entrenamiento'],['Analíticas','Preparado','PDF / entrada estructurada']];return <><header><div className="eyebrow">DATOS</div><h1>Fuentes</h1><p className="muted">{isLiveMode()?'Modo live':'Modo demo/local'}</p></header><section className="card sourceList">{sources.map(([a,b,c])=><div className="source" key={a}><div><strong>{a}</strong><small>{c}</small></div><span>{b}</span></div>)}</section>{user&&<section className="card"><strong>Sesión</strong><p className="muted">{user.email}</p><button className="secondary" onClick={()=>signOut()}>Cerrar sesión</button></section>}</>}