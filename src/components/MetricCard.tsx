type P={label:string;value:string;detail?:string;delta?:number};export default function MetricCard({label,value,detail,delta}:P){return <div className="card metric"><div className="muted">{label}</div><div className="metricRow"><strong>{value}</strong>{delta!==undefined&&<span className={delta>0?'up':'down'}>{delta>0?'+':''}{delta}%</span>}</div>{detail&&<small>{detail}</small>}</div>}