import type{ScientificStudy,Appraisal}from'./types';
export function modelUpdateGate(s:ScientificStudy,a:Appraisal,targetKey:string,kind:string){const score=Math.max(0,Math.min(1,a.score*.55+(s.replicated?1:.35)*.2+(s.hasHardOutcomes?1:.72)*.15+(s.conflicting?.45:1)*.1));const status=score>=.82&&s.replicated&&!s.conflicting?'eligible_for_adoption':score>=.68?'review_required':score>=.52?'candidate':'observe';return{kind,targetKey,gateScore:score,status,requiresHumanReview:true,rationale:[`Appraisal ${a.certainty}`,s.replicated?'Corroborated':'Needs corroboration',s.hasHardOutcomes?'Hard outcomes':'Clinical outcome benefit unresolved','No automatic model mutation']}}
