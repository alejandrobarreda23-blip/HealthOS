import{describe,it,expect}from'vitest';import{METRIC_DICTIONARY_V1}from'../src/health/metrics/dictionary';
describe('Metric Dictionary V1',()=>{it('defines core longitudinal metrics with explicit semantics',()=>{expect(METRIC_DICTIONARY_V1.hrv_rmssd.canonicalUnit).toBe('ms');expect(METRIC_DICTIONARY_V1.hrv_rmssd.sourceEquivalencePolicy).toBe('device_transition_sensitive');expect(METRIC_DICTIONARY_V1.weight.comparisonDirection).toBe('context_dependent');});it('never defines zero-fill missingness for core metrics',()=>{for(const m of Object.values(METRIC_DICTIONARY_V1))expect(m.missingnessPolicy).not.toBe('zero_fill');});});
