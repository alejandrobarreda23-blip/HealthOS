import{describe,it,expect}from'vitest';import{mapAssociationToEffects,aggregateBehaviorImpacts}from'../src/learning/effect-engine';import{OUTCOME_MAP}from'../src/learning/outcome-map';import{deriveOpportunities}from'../src/learning/opportunities';
describe('personal learning loop',()=>{
 it('maps lower HRV after late dinner to unfavorable recovery impact',()=>{const es=mapAssociationToEffects({exposureKey:'late_dinner',outcomeKey:'hrv_rmssd',effect:-.2,nExposed:25,nControl:40,confidence:.82,confounderCoverage:.8,replicationCount:2,window:'same_night'},OUTCOME_MAP);expect(es.find(x=>x.systemKey==='sleep_recovery')!.favorableEffect!).toBeLessThan(0)});
 it('maps lower resting HR after sauna as favorable',()=>{const es=mapAssociationToEffects({exposureKey:'sauna',outcomeKey:'resting_hr',effect:-.12,nExposed:20,nControl:30,confidence:.75,confounderCoverage:.75,replicationCount:1,window:'overnight'},OUTCOME_MAP);expect(es.find(x=>x.systemKey==='cardiovascular')!.favorableEffect!).toBeGreaterThan(0)});
 it('recommends reduction only from sufficient personal evidence',()=>{const es=mapAssociationToEffects({exposureKey:'late_dinner',outcomeKey:'hrv_rmssd',effect:-.25,nExposed:25,nControl:40,confidence:.85,confounderCoverage:.8,replicationCount:2,window:'same_night'},OUTCOME_MAP);const o=deriveOpportunities(aggregateBehaviorImpacts(es))[0];expect(o.action).toBe('reduce')});
});