import{describe,it,expect}from'vitest';import{buildBaselineSnapshotV1,buildDualBaselineV1,contextualBaselineStatus,robustZV1}from'../src/health/baselines/engine';
const rows=Array.from({length:42},(_,i)=>({date:`2026-06-${String(i+1).padStart(2,'0')}`,value:70+(i%5)})).filter(r=>Number(r.date.slice(-2))<=30);
describe('Baseline V1',()=>{it('requires coverage and sample sufficiency',()=>{const b=buildBaselineSnapshotV1('hrv_rmssd','2026-06-30',rows);expect(b.sampleCount).toBe(30);expect(b.sufficient).toBe(true);expect(b.median).not.toBeNull();});it('keeps all-context and reference baselines separate',()=>{const input=rows.map((r,i)=>i<3?{...r,excludedFromReference:true,exclusionReasons:['illness']}:r);const d=buildDualBaselineV1('hrv_rmssd','2026-06-30',input);expect(d.excludedReferenceSamples).toBe(3);expect(d.exclusionReasons.illness).toBe(3);});it('does not invent z when MAD is zero',()=>expect(robustZV1(71,70,0)).toBeNull());it('grades contextual sample sufficiency',()=>{expect(contextualBaselineStatus(9)).toBe('insufficient');expect(contextualBaselineStatus(20)).toBe('usable');expect(contextualBaselineStatus(40)).toBe('established');});});
