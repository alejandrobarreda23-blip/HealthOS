import{describe,it,expect}from'vitest';import{detectInsufficientRecentDataV1,detectSustainedHrvDropV2}from'../src/health/findings-v1/engine';
function date(i:number){const d=new Date('2026-06-01T12:00:00Z');d.setUTCDate(d.getUTCDate()+i);return d.toISOString().slice(0,10);}
describe('Finding Registry V1',()=>{it('detects sustained HRV change without diagnosing cause',()=>{const baseline=Array.from({length:35},(_,i)=>({date:date(i),value:74+(i%3)}));const recent=Array.from({length:7},(_,i)=>({date:date(35+i),value:62+(i%2)}));const f=detectSustainedHrvDropV2(date(41),[...baseline,...recent]);expect(f?.findingKey).toBe('sustained_hrv_drop');expect(f?.interpretationBoundary).toMatch(/No establece/);});it('blocks inference when recent coverage is poor',()=>{const f=detectInsufficientRecentDataV1('2026-08-30',{hrv:{observed:2,expected:7},sleep:{observed:2,expected:7},steps:{observed:3,expected:7}});expect(f?.findingKey).toBe('insufficient_recent_data');expect(f?.coverage).toBeLessThan(.5);});});
