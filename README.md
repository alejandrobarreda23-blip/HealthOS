# HealthOS v1.16.2a — Data JSX hotfix

Full replacement for `src/screens/Data.tsx`.

Fixes the missing closing `)}` around the legacy self-sync card introduced in v1.16.2.
No Supabase migration changes. No rapid-service changes.
